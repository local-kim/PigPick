<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.ReviewMapperInter">
	<select id="getTotalCount" resultType="int">
		select count(*) from review
	</select>
	<select id="getReviewList" resultType="review">
		select review.num as num, member.name as member_name, place_id, stars, content, created_at 
		from review, member 
		where review.member_num = member.num 
		order by created_at desc
	</select>
	<select id="getReview" parameterType="int" resultType="review">
		select * from review where num = #{review_num}
	</select>
	<insert id="insertReview" parameterType="insert_review">
		insert into review (member_num, place_id, stars, content) 
		values (#{member_num}, #{place_id}, #{stars}, #{content})
	</insert>
	<update id="updateReview" parameterType="review">
		
	</update>
	<delete id="deleteReview" parameterType="int">
		delete from review where num=#{num}
	</delete>
</mapper>